import { GeneretePasswordDirective } from './generete-password.directive';

describe('GeneretePasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new GeneretePasswordDirective();
    expect(directive).toBeTruthy();
  });
});
